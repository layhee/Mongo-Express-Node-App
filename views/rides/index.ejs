<!DOCTYPE html>
<html lang="en">
<!---------head--------->
<%- include('../partials/head.ejs')%>
<!---------head--------->
<body>
    <!---------header/nav--------->
    <%- include('../partials/header.ejs')%>
    <!---------header/nav--------->
    <main>
        <h2>Featured Rides</h2><br>
        <dd class="col col-12 d-flex flex-row flex-wrap justify-content-center">
            <%rides.forEach(rides => { %>
                <li class="mx-3 card shadow d-flex align-items-center mb-4 rnr-card">
            <a href="/rides/<%=rides._id%>">
                <img class="card-img-top" src="<%=rides.img%>">
                <div class="d-flex flex-column align-items-center my-4 mx-4">
                    <h5 class="card-title rig-title text-center"><%=rides.name%></a></h5>
                    <p class="card-text p-2 "><%=rides.distance%> kilometers on some <%=rides.terrain%> starting from <%=rides.start%></p>    
                </div>
            </li>
            <% }) %>
        </dd><br>
        <a href="rides/new"><button class="btn btn-warning shadow-sm">Create a Ride</button></a><br>
    </main>
     <!---------footer/animation---------> 
     <%- include('../partials/footer.ejs')%>
     <!---------footer/animation---------> 
</body>
</html>